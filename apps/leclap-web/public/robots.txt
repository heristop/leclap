# https://leclap.pages.dev/robots.txt
User-agent: *
Allow: /

# Private/utility routes carry a `noindex` robots meta (set per-route by the app), so they are
# intentionally NOT disallowed here — a Disallow would block the crawl and the noindex would never
# be seen. Listed for reference: /templates, /templates/*, /projects, /partials.

Sitemap: https://leclap.pages.dev/sitemap.xml
