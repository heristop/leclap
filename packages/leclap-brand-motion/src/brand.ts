// Shared LeClap brand tokens for the motion compositions. The palette matches the app (index.css
// --color-brand-500 / --color-secondary-500 / --color-accent-500) and the clapper bumper.
export const LAVENDER = '#7C83FD';
export const PINK = '#FF8AAE';
export const YELLOW = '#FFE45E';
export const INK = '#0b0b0f';

// The signature lavender→pink brand gradient (logo + primary CTAs), at 135°.
export const BRAND_GRADIENT = `linear-gradient(135deg, ${LAVENDER}, ${PINK})`;
