import 'reflect-metadata';

// Stub — Task 4 fills this in: receive { projectConfig, templateDescriptor } over IPC,
// run the core's compile() (this process owns fd1/fd2 so its logs can't corrupt the
// parent's JSON-RPC stdio), and post back { ok, outputPath } | { ok: false, error }.
export {};
